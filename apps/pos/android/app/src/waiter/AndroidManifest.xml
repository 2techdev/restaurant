<!-- Waiter flavor AndroidManifest overlay.
     Overrides the app label from the main manifest so the installed app
     reads "GastroCore Waiter" instead of "GastroCore POS".
     All permissions, activities, and meta-data are inherited from main/. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:label="@string/app_name"
        tools:replace="android:label" />

</manifest>
