name: gastrocore_models
description: "GastroCore shared domain models, entities, and fare engine. Pure Dart — no Flutter dependency."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  collection: ^1.19.1

dev_dependencies:
  dart_flutter_team_lints: '>=2.0.0 <4.0.0'
  lints: ^5.0.0
  test: ^1.25.0
